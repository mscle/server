var test = require('tap').test;
var traverse = require('../');

test('traverse an Error', function (t) {
    var obj = new Error("tests");
    var results = traverse(obj).map(function (node) {});
    t.same(results, { message: 'tests' });
    
    t.end();
});

